# Code archivé (hors MVP actuel)

Fichiers conservés à titre de référence historique uniquement — **non importés** par l’application.

- `mockDemo.ts` — anciennes données mock avant branchement API Symfony.
- `TimeSlotPicker.tsx` — ancien parcours où l’usager choisissait un créneau (remplacé par attribution système).

Ne pas réintégrer sans revue métier : l’usager ne choisit plus l’heure finale.
